[package]
name = "rlucene"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
[dev-dependencies]
rand = "0.8.5"
bit-set = "0.5.3"

[features]
default= ["test_only"]
test_only = []

