[alias]
xtask = "run --manifest-path xtask/Cargo.toml --"
# format code
tidy  = "xtask tidy"
# Last command before submitting a PR
commit = "xtask commit"
# Github Actions CI
ci = "xtask ci"
# nightly test
nightly = "xtask nightly"
# monster test
monster = "xtask monster"
# Check for uncommitted changes
check-uncommitted = "xtask check-uncommitted"
# Check License header
license-check = "xtask license-check"
# Using Cargo nexttest instead of Cargo test
nextest-run = "xtask nextest-run"
# Run read-only test fixtures in light mode. The environment variable is
# intentionally opt-in; plain `cargo test` keeps per-test setup lifecycles.
test-light = "xtask test-light"
