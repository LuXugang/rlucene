# Maximum line width in characters; lines longer than this will be wrapped.
max_width = 100
# Use spaces instead of tabs for indentation.
hard_tabs = false
# Sort and reorder `use` statements alphabetically.
reorder_imports = true
# Add a trailing comma to the last arm of multi-line `match` expressions.
match_block_trailing_comma = true
# Specify the Rust edition for formatting.
edition = "2024"
# Group imports by standard library, external crates, and local modules.
group_imports = "StdExternalCrate"
# Automatically wrap long comments.
wrap_comments = true
# Use vertical trailing commas in multi-line structures.
trailing_comma = "Vertical"
# Use Unix-style line endings.
newline_style = "Unix"
# Merge multiple `derive` attributes into a single annotation.
merge_derives = true
